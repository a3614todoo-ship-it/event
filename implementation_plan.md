# 藝境空間活動報名系統 - 多圖展示與管理功能實施計畫

本計畫旨在擴充「藝境空間 ‧ 活動報名系統」，讓每個活動能支援最多五張圖片。前台將擺脫單調的單圖顯示，改用極具藝術沙龍感的**「中央大圖展區 + 下方精緻縮圖排」**交互藝廊；後台則升級為**「路徑/本機壓縮上傳雙軌多圖管理區」**，完美兼顧操作直覺與系統效能。

---

## 🎯 視覺設計與互動美學 (Art Gallery Showcase UI)
1.  **大圖展區 (Main Canvas)**：
    *   主圖採用暖金色（`#d97706`）雙線雙層邊框，搭配手作紙張紋理與柔和陰影，營造美術館展牆掛畫的精緻感。
    *   切換圖片時，大圖將進行平滑的淡入淡出（`Fade In/Out`）與輕微縮放過渡，帶來流暢高級的轉場感受。
2.  **下方縮圖排 (Thumbnail Filmstrip)**：
    *   至多五張精緻的小縮圖排成一列。每張縮圖做成微幅傾斜、帶有拍立得或素描邊框的藝術小卡。
    *   **懸停與選取特效**：懸停時小幅擺正並浮現，選中者會套用粗體暖金色卡片邊框與外發光效果，指引極為明確。
3.  **舊活動相容 (Legacy Compatibility)**：
    *   若舊活動僅有一張圖片，系統會自動隱藏下方縮圖列，只顯示單張大圖，保證 100% 舊資料相容。

---

## ✨ 核心技術實作規格

### 1. 後台多圖管理區 (admin.html & admin.js)
*   **多圖配置欄位**：
    *   活動編輯表單中，將原先單一的「封面圖片路徑」欄位，升級為擁有 **5 個獨立圖片槽** 的管理面板。
    *   每個槽位均支援：
        1.  **路徑/網址手動輸入**：可輸入專案相對路徑（如 `assets/art1.jpg`）或外部 CDN 連結。
        2.  **本機拖曳/選取上傳**：點擊可選擇電腦中照片。
*   **前端智慧畫布壓縮 (Canvas Compress)**：
    *   為防止 Base64 體積過大超出 Firestore 單文件 1MB 限制，若管理者選擇本機上傳圖片，JS 會透過動態 `HTML5 Canvas` 將大圖強制重繪壓縮（最大寬高限制 `800px`，畫質設為 `0.6`）。
    *   此壓縮機制可將單張大圖體積控制在 **30KB - 50KB** 之間，即使上傳滿 5 張本機照片，總量也僅約 200KB，能在 Firestore 中極速存取且 100% 不會超出限制！

### 2. 前台詳情頁 (details.html & events.js)
*   **畫廊組件渲染**：
    *   讀取活動資料中的 `images` (String 陣列)。若無此欄位，則自動 fallback 使用舊的 `image` 欄位。
    *   當點擊下方縮圖時，觸發轉場動畫，並即時更新主大圖的 `src` 與其下載/行事曆關聯圖。

---

## 📂 預計修改之檔案

### 1. 後端管理系統
*   #### [MODIFY] [admin.html](file:///d:/AI/standalone-events/admin.html)
    *   將「封面圖片路徑」輸入欄位替換為「五張活動圖片槽面板」，每個槽位配有文字框、上傳鈕與即時微縮圖。
*   #### [MODIFY] [admin.js](file:///d:/AI/standalone-events/admin.js)
    *   實作圖片選取與 Canvas 壓縮（`FileReader` + `Image` + `Canvas`）邏輯。
    *   在儲存活動資料（`save`）時，將 5 個槽位整合為一個 `images` 陣列（同時將第一個槽位寫入舊 `image` 欄位確保前台相容），寫入 Firestore。
    *   在編輯活動資料（`editEvent`）載入時，將資料庫中的圖片陣列還原至 5 個槽位中。

### 2. 前台門戶系統
*   #### [MODIFY] [details.html](file:///d:/AI/standalone-events/details.html)
    *   調整左側活動主視覺區域的 HTML 結構，移除單一 `<img>`，改為 `event-gallery-container`，包含主大圖區與下方的縮圖滑軌容器。
*   #### [MODIFY] [details.css](file:///d:/AI/standalone-events/details.css) / [styles.css](file:///d:/AI/standalone-events/styles.css)
    *   為大圖展區與下方精緻拍立得縮圖排撰寫專屬的 Vanilla CSS。包含雙線暖金色邊框、旋轉微動畫、選中外發光及平滑轉場效果。
*   #### [MODIFY] [events.js](file:///d:/AI/standalone-events/events.js)
    *   在 `renderUI()` 中動態讀取 `images` 陣列。
    *   動態渲染縮圖排 HTML 並綁定切換大圖的 click 監聽器，實作優雅的淡入淡出（`Fade`）轉場過渡。

---

## 🧪 驗證與測試計畫
1.  **壓縮測試**：分別上傳 5 張數 MB 大小的相機原圖，確認是否皆被自動壓縮至 50KB 以下，且在後台能正常顯示五個微縮圖。
2.  **儲存與載入測試**：儲存後，確認 Firebase 中的 `images` 陣列長度為 5，且再次點擊「編輯」時，5 張圖的路徑/Base64 能完美回填至表單。
3.  **前台展示與切換測試**：開啟活動詳情頁，確認五張縮圖正確渲染，點擊每一張縮圖時，大圖是否伴隨平滑淡入動畫切換，且僅有單圖的舊活動能正常 fallback 隱藏縮圖排。
